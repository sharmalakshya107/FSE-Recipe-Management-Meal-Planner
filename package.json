{
  "name": "recipe-meal-planner",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend & npm run dev -w backend",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w frontend -w backend -w shared"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5"
  },
  "overrides": {
    "tar": "^7.5.7",
    "@mapbox/node-pre-gyp": {
      "tar": "^7.5.7"
    }
  }
}
